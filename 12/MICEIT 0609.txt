(sudo) npm install -g json-server

$.post(~~~~~~~~~~~~~

12\gulp-test-master > npm install

npm install --global gulp-cli // 아무데서나 걸프명령어를 사용할 수 있게됨


cmd >> gulp

gulp default

gulp default



걸프의 특정 기능을 사용하고싶다면 모듈로 설치를 해야됨
concat:js
gulpfile.js


gulp - src = 아무런 처리를 하지 않은 JavaScript와 압축하지 않은 css,
		최적화하지 않은 이미지들이 들어감
gulp - dis = gulp에 의해 빌드된 파일이 들어감

gulp.task('copy', ['copy:html', 'copy:lib']);
//gulp copy 실행시 copy:html과. copy:lib 동시실행(묶음)


package.json이 js마다 있으며 gulp를 추가해서 사용.

--------------------------------------------------------

git에서 gulp 마지막것 다운로드
cmd - npm install








지금은 전체데이터를 다 저장해서 다 가져옴
->개별로 등록할때마다 날리기(저장)

todo에서 입력한 내용을 로컬스토리지가 아닌
3천번 포트에 떠있는 json 서버에 저장하기



$.get('http://localhost:3000/todos', function(result){
	return result;
	});



>>main.js



json.server에서 create 누르면 모델 추가(리스트에 등록)






json.server 구현(localhost:3000) -> 서버와 연동